# Script that creates some basic directories to run the crawler
# credentials: foursquare user credentials that will be used in her behalf by the crawler.
# data: contains files with the ids of users from foursquare who will be crawled.
#output: where all crawled data will be saved
#log: where all log will be stored
mkdir ../credentials
mkdir ../data
mkdir ../output
mkdir ../log
